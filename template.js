const firstName ='Justin';
const lastName = 'Tmberlake';
const fullName = firstName +" " + lastName + ' is good boy';

const fullName2 = `${firstName} ${20+15+30} is a good boy`

const multiLine = 'go where you want to\n'
+'otherwise stay hone\n'
+'F off'

const multiLine2 = `go where want to 
    otherwise stay home 
    or F off`

console.log(multiLine2);